# Provenance

`SHA256SUMS` lists the SHA-256 of the model's final `Spec.lean` as verified in the run. `MeanValueProblem.diff` is the unified diff from that file to `MeanValueProblem/Resolutions/MeanValueProblem.lean`.
