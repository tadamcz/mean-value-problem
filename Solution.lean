import MeanValueProblem.Resolutions.MeanValueProblem

/-!
# Proved solution

Comparator checks that `MeanValueProblem.mean_value_problem.disproof` has exactly the same statement here as in
`Challenge.lean` and that its proof uses only the permitted axioms. The theorem is proved in the
imported module `MeanValueProblem.Resolutions.MeanValueProblem`, which is the file the AI system produced
(see the README for the exact edits made to it). This module adds no declarations of its own: the
compared theorem is the one in the imported module's environment.
-/
